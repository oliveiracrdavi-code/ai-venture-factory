# segredos - NUNCA versionar
.env
.env.*
*.pem
*.key

# node
node_modules/
npm-debug.log*

# SO / editor
.DS_Store
Thumbs.db
*.swp

# estado derivado (regenerado por scripts/activate.js e snapshot.js)
company/state/*.json

# log de atividade (append-only, cresce a cada tool call - nao versionar,
# senao cada commit gera uma nova entrada e um loop infinito de commits)
company/logs/events.jsonl

# artefatos de execucao do piloto (descomente se nao quiser versionar as rodadas)
# company/projects/app-*/
# company/tasks/TASK-*.md
# company/logs/chats/*.jsonl
